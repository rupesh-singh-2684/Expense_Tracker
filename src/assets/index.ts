const pathToIcons = '../assets/icons'
const pathToImages = '../assets/images'
const pathToVideos = '../assets/videos'

const Icons = {
    homeMenu:require(`${pathToIcons}/menu1.png`),
    homeComments:require(`${pathToIcons}/chat2.png`),
    homeAdd:require(`${pathToIcons}/add.png`),
    manualCreate:require(`${pathToIcons}/expenses.png`),
    camera:require(`${pathToIcons}/camera.png`),
    clock:require(`${pathToIcons}/clock1.png`),
    remove:require(`${pathToIcons}/remove.png`),
    income:require(`${pathToIcons}/income1.png`),

    // homeNotification:require(`${pathToIcons}/love.png`),
    // homeMessenger:require(`${pathToIcons}/messenger.png`),
    // comments:require(`${pathToIcons}/comment.png`),
    // share:require(`${pathToIcons}/share.png`),
}

const Images = {
    // profileImage:require(`${pathToImages}/profile.jpg`),
    // // profileImage1:require(`${pathToImages}/profile1.webp`),
    // profileImage2:require(`${pathToImages}/profile1.jpeg`),
    // // profileImage3:require(`${pathToImages}/profile3.webp`),
    // profileImage4:require(`${pathToImages}/profile3.jpeg`),

}

const Videos = {
    // testingVideo:require(`${pathToVideos}/bgvp.mp4`)
}

export {Icons,Images,Videos};